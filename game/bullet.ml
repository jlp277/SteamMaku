module Bullet = struct

let check_contacts (data : game_data) : player_char list * player_char list =
  (* check if invincible *)
  (* output should have no duplicate players *)
  failwith "lkdfj"
end